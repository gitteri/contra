# Contra chain RPC (used as fallback if read/write aren't set)
CONTRA_RPC_URL=http://localhost:8899

# Optional: split read/write endpoints for production
# CONTRA_READ_URL=https://read.onlyoncontra.xyz
# CONTRA_WRITE_URL=https://write.onlyoncontra.xyz
